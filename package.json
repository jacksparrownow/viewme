{
  "name": "viewmeasap",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp _headers dist/_headers",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
